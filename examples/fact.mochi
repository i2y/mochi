from time import time

def factorial(n, m):
    if n == 1:
        m
    else:
        factorial(n - 1, n * m)

start = time()
print(factorial(10000, 1))
print()
print('factorial(10000)')
print('time:', time() - start)
